import type { EventInputs } from "@/lib/formulas/event";

export const defaultEventInputs: EventInputs = {
  eventFormat: "webinar",
  industryVertical: "general-b2b",
  campaignService: "pre-post",
  currency: "EUR",
  signupTarget: 250,
  budgetPerSignup: 60,
  averageNewClientROI: 12000,
};
