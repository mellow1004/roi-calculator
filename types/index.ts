/**
 * Barrel for calculator/domain types. `CalculatorStep` is defined in
 * `./calculator` and includes `"confirmation"` between `"your-details"` and
 * `"results"`.
 */
export * from "./calculator";
export type { CampaignService, EventFormat, IndustryVertical } from "@/lib/formulas/event";
